# Site settings
title: rustaceans.cologne
description: "Rust User Group in the area of Cologne/Bonn, Germany"
baseurl: "https://killercup.github.io/rust-cologne"
url: "http://rustaceans.cologne/"
twitter_username: rustcologne
github_username:  killercup

# Build settings
permalink: "/:year/:month/:day/:title.html"
markdown: kramdown
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "event"

# Content settings
future: true # we use dates for the actual meetup time, not the post publication time
timezone: "Europe/Berlin"